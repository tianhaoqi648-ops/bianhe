# 辩盒 v1.2.3 更新内容

## 修复

- **修复「关于辩盒」弹窗中版本号显示错误**：v1.2.2 版本的应用内仍显示为 v1.2.1，现已修复，版本号将自动与当前版本保持一致。

# 辩盒 v1.2.2 更新内容

## 修复

- **修复部分用户首次安装或更新后启动闪退**：解决了数据库初始化时因缺少字段导致应用启动失败的问题（错误提示"no such column: stage_remaining_cache"）。新安装或跨版本更新的用户不会再遇到此问题。