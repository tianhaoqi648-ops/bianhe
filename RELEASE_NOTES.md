# 辩盒 v1.6.0 更新内容

## 稳定性与性能
- **赛事管理提速**：赛事卡片统计（轮数/队伍数/已完成轮数）改为一次批量计算，去除原来「N 个赛事 × 3 组查询」的慢查询
- **Agent 更可控**：Agent 工具分「读 / 写 / 危险」三级、**默认只读**，写/危险操作需用户确认后执行
- **构架清理**：合并两套 preload 路径，只保留一套明确结构

## 出错更可控
- **统一错误分类**：内部异常不再直接抛给用户，改为可读中文提示（如「该辩题已存在」），并保留开发者日志
- **修复静默失败**：导入关联、Agent 会话恢复失败不再只写日志；批量操作的部分失败会给用户明确反馈

## 升级更安全
- **数据库迁移加固**：引入数值 schema 版本，迁移按序事务执行、失败回滚不落半状态、可重复执行；结构升级前自动备份快照，恢复时校验版本，**数据不丢失**

## 操作可撤销
- **撤销/重做覆盖面扩大**：批量编辑辩题、标签批量修改、赛制/赛事/队伍修改均可撤销与重做；新操作会清空重做栈（标准 Undo/Redo 语义）

## 可靠性
- **核心自动测试补强**：抽题、计时、去重、导入、备份、迁移等引擎测试补齐；Agent 增加失败处理与「多会话互不污染」专项测试

## 修复
- **Agent 假成功**：工具返回失败标识时不再误判为成功并反馈给模型
- **损坏文件明确报错**：导入损坏或加密的 Excel 时给出明确提示，而非静默空结果

---

# 辩盒 v1.5.5 更新内容

## 新功能
- **AI 裁判三角色**：工作台分「裁判 / 陪练 / 复盘」。裁判＝双方评审（五维判分 + 整场/分环节），陪练＝模拟对手回合/实时对辩，复盘＝教练给短板与可练方向
- **整场评审更顺**：裁判可绑定赛事-场次，载入本场录音自动转写、自动归边后整场评审，整段粘贴整场稿亦可；单环节评审另作独立入口
- **实时对辩**：陪练支持实时对辩（申论→质询→自由辩论→总结），可文字或麦克风语音回应，结束给出对抗要点
- **陪练分环节**：可锁定某一环节（申论/质询/自由辩论/总结）针对性练，对手只在该环节内应对
- **复盘分环节**：新增整场分环节复盘，按环节逐段给出四维短板、可练方向与示范改写，并附整场汇总
- **评委匿名化**：评委不再展示真人名，仅保留「攻防流 / 价值流 / 学理流 / 建构流」等风格原型

## 改进
- **录音更安全**：录音存储改为「数据根 /recordings」，更新/重装/卸载不再误删录音；每场可绑定多份录音（整段或按环节）
- **转写更稳**：非 wav 转码失败时会自动改走已配置的 AI 转写 API，不再被 ffmpeg 卡住
- **历史可回看**：修复 AI 裁判历史「查看」偶发无内容，任意记录展开都有结果（含实时对辩轮次）
- **抽取仪式更轻**：首题渐显、字号统一、长题自动换行，去除 3D 翻牌

---

# 辩盒 v1.5.3 更新内容

## 新功能
- **辩题题库管理**：可按「题库」归类整理辩题（新建/重命名/删除、整库复制/移动到其它题库、多选批量加入/移出），题库内支持搜索与维度筛选
- **赛事选题更灵活**：赛事可绑定多个题库，并选择四种选题方式（单一题库 / 绑定并集 / 带优先级的顺序后备 / 按轮次指定）；中途更换题库时若含已抽辩题会及时提醒
- **加题更方便**：赛事配题可直接在弹出的列表里搜索/筛选/快速新建辩题（可一并入库）并标记已抽状态；批量导入候选支持「全选」
- **抽取仪式更轻**：首题揭晓改为轻渐显，去掉突兀的 3D 翻牌；各题字号统一，长题自动换行，不再造突兀大字或互相遮挡

## 改进
- **赛事包更完整**：赛事导出/导入随附「赛事→题库绑定、轮次→题库绑定」及被引用题库定义，迁移不丢选题配置
- **备份更周全**：备份/迁移已纳入题库、轮次题库绑定与赛事选题配置
- **管理面板更稳**：题库管理弹窗改为弹窗内滚动、操作按钮自动换行，内容再多也不会被裁掉

---

# 辩盒 v1.5.2 更新内容

## 新功能
- **计时器更贴心**：支持赛制级「每队总时长池」（各环节共用，池尽即止）、自由辩论自动累计双方发言次数；关键时间点可选自定义铃声
- **复盘可视化·五维雷达图**：AI 裁判结果以五维能力雷达图直观展示双方强弱
- **一键导出复盘报告**：录音评审后可一键导出 Markdown 或自包含 HTML 复盘（含雷达图、逐环节点评、胜负与建议），便于存档分享
- **赛题抽取更有仪式感**：抽取时进入全屏沉浸动画（滚动/定格/音效），空格或翻页笔一键开始与停止
- **辩题库关键词搜索**：按关键词/标签快速筛选题库
- **赛程 Excel 导入导出 + 队徽库**：赛程可导出为 Excel（导入前有变更预览确认）；内置校徽并可上传，给队伍一键绑定队徽
- **铃声库与超时语音**：内置多套预设铃声一键切换；到点/超时可选择本地语音播报并可调音量
- **大屏显示更完整**：带总时长池的赛制在大屏同时显示双方剩余池，字号自适应不遮挡

## 改进
- **AI 裁判评审留痕**：所有评审结果自动保存为历史记录，切页或重启后仍可回看、重开，并一键写回该场
- **从赛事直达 AI 裁判**：赛事赛程可直接打开 AI 裁判工作台并自动定位当前赛事-轮次-场次
- **AI 助手更懂新功能**：助手可帮你导出/导入赛程 Excel、给队伍绑定队徽；其评审结果同样记入评审历史
- **备份更完整**：备份/恢复现包含 AI 裁判历史与队徽库，换机迁移不丢数据
- **修复并优化了一批已知问题**：改善多处使用体验细节，整体运行更稳定

---

# 辩盒 v1.5.1 更新内容

## 修复
- **修复更新后丢失转写模型与转码器**：应用更新升级时不再清空已下载的转写模型（whisper / FunASR）与 ffmpeg 转码器，升级后无需重新下载
- **转写数据目录更智能**：自定义存放位置后，转写数据会自动存到该位置下的 stt 子目录，无论选什么文件夹名都能在更新时被保留

---

# 辩盒 v1.5.0 更新内容

## 新功能
- **AI 裁判·整场评审**：在裁判工作台把本场录音一键转成带环节分段的文字，即可让 AI 评委基于整场内容做胜负判定、五维评分、最佳辩手推荐与风格化点评
- **录音转文字**：本场录音支持一键转写，自动按辩论环节分段，作为整场评审与环节分析的素材直接使用
- **本地转写引擎可切换**：支持 whisper.cpp 与阿里 FunASR 两种本地引擎，可按需切换；缺依赖或未安装 Python 时可一键引导安装，也可选择更大的模型让中文识别更准

## 改进
- **素材不足时如实告知**：当录音过短、或内容不足以支撑胜负判定时，AI 裁判会明确提示「无法判定」并说明原因，不再凭空生成一份评分

## 修复
- 修复整场评审在未填写辩题时无法执行的问题
- 修复素材不足时误将赛果写成「平局」的问题
- 修复整场评审结果卡片内容空白的问题

---

# 辩盒 v1.4.1 更新内容

## 修复
- **修复赛制编辑器「无法添加环节」**：新建赛制后提示"请添加环节"时，现在会直接显示「添加环节」按钮，点击即可新增环节
- **修复计时器大屏正反方显示不全**：调整中央倒计时圈尺寸，矮窗口下正反方名称不再被顶出屏幕，可完整显示

---

# 辩盒 v1.4.0 更新内容

## 新功能
- **AI 裁判工作台**：全新独立页面（菜单「比赛工具 → AI 裁判」）——选择评委风格、粘贴或上传辩词（支持 .txt/.md/.docx），一键完成单方评审、双方评审、模拟对方攻击与环节识别，让备赛评估更高效
- **评委风格化**：提供攻防流 / 价值流 / 价值+知识 / 学理流 / 建构流 5 种评审风格，每种风格附详细说明，可按需要选择最贴合比赛场景的评审视角
- **切换会话不中断 AI 回复**：切换会话时，当前回答在后台继续完成，回到原会话即可看到完整结果；支持多会话并行使用
- **稿子文件上传**：可直接上传 .txt/.md/.docx 辩词文件，内容自动填入输入框，无需手动复制粘贴

## 改进
- 评审操作更直观：「单方评审」评估一方辩词（五维评分 + 漏洞清单 + 改进建议）；「双方评审」对比正反方完整辩词（胜负判定 + 五维对比）
- 聊天记录与裁判工作台的结果展示方式保持一致，阅读更统一

## 修复
- 修复部分场景下应用启动白屏的问题
- 修复切换会话后 AI 回复被中断、不完整的问题

---

# 辩盒 v1.3.0 更新内容

## 新功能
- **AI Agent 助手**：新增自然语言驱动的智能助手，支持通过对话完成辩题搜索、抽取、赛事创建等操作
- **多会话管理**：支持创建、切换、重命名、删除多个独立会话，每个会话保留独立上下文
- **人工确认机制**：高风险工具调用前弹窗确认，支持修改参数后执行
- **对话导出**：支持将对话导出为 Markdown 或 JSON 格式
- **历史检索**：跨会话搜索历史对话内容
- **上下文锁定**：锁定后切换页面不会覆盖当前上下文
- **赛事批量导入工具**：通过对话从 Excel/CSV 文件批量导入赛事与队伍
- **赛制推荐工具**：根据队伍数、时长、风格推荐合适的赛制模板
- **队伍分组优化工具**：支持蛇形平衡或随机种子策略优化分组
- **赛程自动排表工具**：支持单淘汰/单循环/双淘汰/瑞士轮四种赛制自动生成赛程

## 改进
- 设置页新增「AI 助手」配置项，支持配置 OpenAI/通义千问/Kimi/智谱清言/DeepSeek 等服务商
- 设置页新增「工具确认规则」配置，可自定义每个工具是否需要人工确认
- Agent 面板支持快捷键 Ctrl+J 折叠/展开
- **会话记忆隔离**：每个会话的 AI 上下文独立保存，切换会话互不串扰，重启应用后仍记得聊过的内容
- **提问即建会话**：直接在面板输入问题会自动创建并保存会话，对话记录不再丢失
- **切换会话即时停止**：切换或删除会话时自动停止正在进行的 AI 回答，不会串台
- **回答中断一键重试**：网络异常或限流导致回答中断时，可一键重新发送
- **对话自动滚动**：流式回答过程中自动跟随滚动，长回复无需手动拖到底部

## 修复
- **历史会话工具卡片恢复**：重新打开历史会话时，工具调用卡片（搜索、抽取、建赛事等）恢复正常显示
- **多轮工具操作合并显示**：一次对话中的多次工具操作合并为一条完整回复，不再分段显示

---

# 辩盒 v1.2.3 更新内容

## 修复

- **修复「关于辩盒」弹窗中版本号显示错误**：v1.2.2 版本的应用内仍显示为 v1.2.1，现已修复，版本号将自动与当前版本保持一致。

---

# 辩盒 v1.2.2 更新内容

## 修复

- **修复部分用户首次安装或更新后启动闪退**：解决了数据库初始化时因缺少字段导致应用启动失败的问题（错误提示"no such column: stage_remaining_cache"）。新安装或跨版本更新的用户不会再遇到此问题。

---

# 辩盒 v1.2.1 更新内容

## 修复

- **修复抽取页面无赛事时显示多余空状态**：之前在抽取页面下滑会看到「暂无赛事，请先创建」的提示，即使已有赛事也会误显示。现已移除该空状态，仅保留引导步骤。
- **修复侧边栏版本号显示错误**：侧边栏底部此前固定显示 `v1.0.0`，现已改为从应用版本动态读取，与当前版本一致。
- **自动更新完成后清理安装包**：自动更新下载的安装包（约 400MB）原会残留在用户磁盘，现已在应用启动时自动清理上次更新残留的安装包，避免多次更新后磁盘空间持续累积。
