# 辩盒 v1.2.1 更新内容

## 修复

- **修复抽取页面无赛事时显示多余空状态**：之前在抽取页面下滑会看到「暂无赛事，请先创建」的提示，即使已有赛事也会误显示。现已移除该空状态，仅保留引导步骤。
- **修复侧边栏版本号显示错误**：侧边栏底部此前固定显示 `v1.0.0`，现已改为从应用版本动态读取，与当前版本一致。
- **自动更新完成后清理安装包**：自动更新下载的安装包（约 400MB）原会残留在用户磁盘，现已在应用启动时自动清理上次更新残留的安装包，避免多次更新后磁盘空间持续累积。
